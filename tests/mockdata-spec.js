const { expect } = require('chai');
const random = require('../database/data-generator.js');

describe('Test mock data functions', () => {
  it('should generate a random sessions', (done) => {
    // const sessions = random.generateRandomSession();
    // expect(sessions.hash).to.have.lengthOf.above(34);
    // expect(sessions.hash).to.be.a('string');
    done();
  });
});